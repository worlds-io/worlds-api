enum DataSourceType {
    FOLDER,
    VIDEO_DEVICE,
    IMAGE_DEVICE
    GEOPOSITION
}

type DataSource {
    id: ID!
    name: String!
    type: DataSourceType!
    device: Device
}

input CreateDataSourceInput {
    name: String!
    type: String!
}